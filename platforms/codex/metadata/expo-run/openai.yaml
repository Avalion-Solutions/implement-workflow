interface:
  display_name: "Expo Run"
  short_description: "Launch browser and Expo Go with a terminal QR"
  default_prompt: "Use $expo-run to configure or launch this Expo project with one tunnel that verifies browser HTTPS and Expo Go exp links, then prints a terminal QR report."
